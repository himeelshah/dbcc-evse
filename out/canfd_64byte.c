/* Generated by DBCC, see <https://github.com/howerj/dbcc>

This file was generated by dbcc: See <https://github.com/howerj/dbcc>

For the professional version of this utility visit https://subleq.co.uk
The professional version includes support for CAN-FD, more languages, and
better MISRA-C support.


*/

#include "canfd_64byte.h"
#include <inttypes.h>
#include <string.h>
#include <assert.h>

#define UNUSED(X) ((void)(X))

static inline uint64_t reverse_byte_order(uint64_t x) {
	x = (x & 0x00000000FFFFFFFF) << 32 | (x & 0xFFFFFFFF00000000) >> 32;
	x = (x & 0x0000FFFF0000FFFF) << 16 | (x & 0xFFFF0000FFFF0000) >> 16;
	x = (x & 0x00FF00FF00FF00FF) << 8  | (x & 0xFF00FF00FF00FF00) >> 8;
	return x;
}

static inline int print_helper(int r, int print_return_value) {
	return ((r >= 0) && (print_return_value >= 0)) ? r + print_return_value : -1;
}

static int pack_can_0x001_CANFDMessage(can_obj_canfd_64byte_h_t *o, uint8_t *data) {
	assert(o);
	assert(data);
	register uint64_t x;
	register uint64_t i = 0;
	/* Signal2: start-bit 24, length 32, endianess intel, scaling 1, offset 0 */
	x = ((uint32_t)(o->can_0x001_CANFDMessage.Signal2)) & 0xffffffff;
	x <<= 24; 
	i |= x;
	/* Signal1: start-bit 8, length 16, endianess intel, scaling 1, offset 0 */
	x = ((uint16_t)(o->can_0x001_CANFDMessage.Signal1)) & 0xffff;
	x <<= 8; 
	i |= x;
	/* Signal0: start-bit 0, length 8, endianess intel, scaling 1, offset 0 */
	x = ((uint8_t)(o->can_0x001_CANFDMessage.Signal0)) & 0xff;
	i |= x;
	{ uint64_t _packed = (i); memcpy(data, &_packed, sizeof(_packed)); }
	o->can_0x001_CANFDMessage_tx = 1;
	return 64;
}

static int unpack_can_0x001_CANFDMessage(can_obj_canfd_64byte_h_t *o, const uint8_t * data, uint8_t dlc, dbcc_time_stamp_t time_stamp) {
	assert(o);
	assert(dlc <= 64);
	register uint64_t x;
	uint64_t _raw; memcpy(&_raw, data, sizeof(_raw));
	register uint64_t i = (_raw);
	if (dlc < 64)
		return -1;
	/* Signal2: start-bit 24, length 32, endianess intel, scaling 1, offset 0 */
	x = (i >> 24) & 0xffffffff;
	o->can_0x001_CANFDMessage.Signal2 = x;
	/* Signal1: start-bit 8, length 16, endianess intel, scaling 1, offset 0 */
	x = (i >> 8) & 0xffff;
	o->can_0x001_CANFDMessage.Signal1 = x;
	/* Signal0: start-bit 0, length 8, endianess intel, scaling 1, offset 0 */
	x = i & 0xff;
	o->can_0x001_CANFDMessage.Signal0 = x;
	o->can_0x001_CANFDMessage_rx = 1;
	o->can_0x001_CANFDMessage_time_stamp_rx = time_stamp;
	return 64;
}

int decode_can_0x001_Signal2(const can_obj_canfd_64byte_h_t *o, uint32_t *out) {
	assert(o);
	assert(out);
	uint32_t rval = (uint32_t)(o->can_0x001_CANFDMessage.Signal2);
	*out = rval;
	return 0;
}

int encode_can_0x001_Signal2(can_obj_canfd_64byte_h_t *o, uint32_t in) {
	assert(o);
	o->can_0x001_CANFDMessage.Signal2 = in;
	return 0;
}

int decode_can_0x001_Signal1(const can_obj_canfd_64byte_h_t *o, uint16_t *out) {
	assert(o);
	assert(out);
	uint16_t rval = (uint16_t)(o->can_0x001_CANFDMessage.Signal1);
	*out = rval;
	return 0;
}

int encode_can_0x001_Signal1(can_obj_canfd_64byte_h_t *o, uint16_t in) {
	assert(o);
	o->can_0x001_CANFDMessage.Signal1 = in;
	return 0;
}

int decode_can_0x001_Signal0(const can_obj_canfd_64byte_h_t *o, uint8_t *out) {
	assert(o);
	assert(out);
	uint8_t rval = (uint8_t)(o->can_0x001_CANFDMessage.Signal0);
	*out = rval;
	return 0;
}

int encode_can_0x001_Signal0(can_obj_canfd_64byte_h_t *o, uint8_t in) {
	assert(o);
	o->can_0x001_CANFDMessage.Signal0 = in;
	return 0;
}

int print_can_0x001_CANFDMessage(const can_obj_canfd_64byte_h_t *o, FILE *output) {
	assert(o);
	assert(output);
	int r = 0;
	r = print_helper(r, fprintf(output, "Signal2 = (wire: %.0f)\n", (double)(o->can_0x001_CANFDMessage.Signal2)));
	r = print_helper(r, fprintf(output, "Signal1 = (wire: %.0f)\n", (double)(o->can_0x001_CANFDMessage.Signal1)));
	r = print_helper(r, fprintf(output, "Signal0 = (wire: %.0f)\n", (double)(o->can_0x001_CANFDMessage.Signal0)));
	return r;
}

int unpack_message(can_obj_canfd_64byte_h_t *o, const unsigned long id, const uint8_t * data, uint8_t dlc, dbcc_time_stamp_t time_stamp) {
	assert(o);
	assert(id < (1ul << 29)); /* 29-bit CAN ID is largest possible */
	assert(dlc <= 64);        /* Maximum of 64 bytes in a CAN FD packet */
	switch (id) {
	case 0x001: return unpack_can_0x001_CANFDMessage(o, data, dlc, time_stamp);
	default: break; 
	}
	return -1; 
}

int pack_message(can_obj_canfd_64byte_h_t *o, const unsigned long id, uint8_t *data) {
	assert(o);
	assert(id < (1ul << 29)); /* 29-bit CAN ID is largest possible */
	switch (id) {
	case 0x001: return pack_can_0x001_CANFDMessage(o, data);
	default: break; 
	}
	return -1; 
}

int message_dlc(const unsigned long id) {
	assert(id < (1ul << 29)); /* 29-bit CAN ID is largest possible */
	switch (id) {
	case 0x001: return 64;
	default: break; 
	}
	return -1; 
}

int print_message(const can_obj_canfd_64byte_h_t *o, const unsigned long id, FILE *output) {
	assert(o);
	assert(id < (1ul << 29)); /* 29-bit CAN ID is largest possible */
	assert(output);
	switch (id) {
	case 0x001: return print_can_0x001_CANFDMessage(o, output);
	default: break; 
	}
	return -1; 
}

